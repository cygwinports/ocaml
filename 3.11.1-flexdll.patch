Thanks, but no thanks, we really do NOT want to define _WIN32 on Cygwin.

--- origsrc/ocaml-3.11.1/configure	2009-05-20 10:33:09.000000000 -0500
+++ src/ocaml-3.11.1/configure	2009-07-21 16:12:39.697949900 -0500
@@ -302,7 +302,7 @@ case "$bytecc,$host" in
     bytecccompopts="-D_XOPEN_SOURCE=500";;
   gcc*,*-*-cygwin*)
     bytecccompopts="-fno-defer-pop $gcc_warnings -U_WIN32"
-    dllccompopts="-D_WIN32 -DCAML_DLL"
+    dllccompopts="-DCAML_DLL"
     flexlink="flexlink -chain cygwin -merge-manifest"
     flexdir=`$flexlink -where | dos2unix`
     if test -z "$flexdir"; then
--- origsrc/ocaml-3.11.1/otherlibs/labltk/support/camltk.h	2008-09-26 02:35:24.000000000 -0500
+++ src/ocaml-3.11.1/otherlibs/labltk/support/camltk.h	2009-07-21 16:00:03.010136200 -0500
@@ -16,7 +16,7 @@
 
 /* $Id: camltk.h,v 1.13 2008/09/26 07:35:24 garrigue Exp $ */
 
-#if defined(_WIN32) && defined(CAML_DLL) && defined(IN_CAMLTKSUPPORT)
+#if defined(CAML_DLL) && defined(IN_CAMLTKSUPPORT)
 #define CAMLTKextern CAMLexport
 #else
 #define CAMLTKextern CAMLextern
